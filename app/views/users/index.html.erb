<p style="color: green"><%= notice %></p>

<% content_for :title, "Users" %>

<h1>Users</h1>

<div id="users">
  <ul>
  <% @users.each do |user| %>
    <li><%= user.email %> <%= link_to "Show this user", user %></li>
  <% end %>
  </ul>
</div>

<%= link_to "New user", new_user_path %>
