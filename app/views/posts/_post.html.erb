<div id="<%= dom_id post %>">
  <p>
    <strong>Post author:</strong>
    <%= link_to post.user.email, user_path(post.user) %>
    <% if post.user_id == current_user.id %>
      <span class="your-post">IT's YOUR POST!</span>
    <% end %>
  </p>
  <p>
    <strong>Post content:</strong>
    <%= post.content %>
  </p>

</div>
